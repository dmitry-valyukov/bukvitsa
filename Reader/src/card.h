#pragma once
// Карточка кнопок: общая обстановка стартового экрана и мастера обложек.
//
// Параметры — от общепринятой Card (HelloHere в примерах wxl): скругление,
// рамка, ThemeShadow с подъёмом по Z. Лицо не белое, а чёрно-коричневое и
// сильно полупрозрачное: карточка ложится на заставку или на снимок книги и
// не должна их глушить.

#include "pch.h"

namespace bukvitsa::reader {

/// Строит карточку вокруг содержимого и ставит её туда, где стоят кнопки
/// стартового экрана, — в правый верхний угол.
wxl::Border buttonCard(const wxl::UIElement& content);

}  // namespace bukvitsa::reader
