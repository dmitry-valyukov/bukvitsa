#pragma once
// Карточка кнопок: общая обстановка стартового экрана и мастера обложек.
//
// Вид берётся у библиотечной wxl::Card; здесь только то, чем читалка от неё
// отличается, — место в углу экрана и лицо. Оно не белое, а чёрно-коричневое
// и сильно полупрозрачное: карточка ложится на заставку или на снимок книги
// и не должна их глушить.

#include "pch.h"

namespace bukvitsa::reader {

/// Строит карточку вокруг содержимого и ставит её туда, где стоят кнопки
/// стартового экрана, — в правый верхний угол.
wxl::Border buttonCard(const wxl::UIElement& content);

}  // namespace bukvitsa::reader
